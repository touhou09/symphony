# Copy to .env or export these variables before `docker compose up`.
SYMPHONY_SOURCE_REPO=https://github.com/touhou09/symphony
SYMPHONY_SOURCE_BRANCH=dev
SYMPHONY_PR_REPO=touhou09/symphony
SYMPHONY_PR_BASE=dev

# GitHub token used by gh/git during workspace PR publication.
GH_TOKEN=
# Optional fallback if GH_TOKEN is not set.
GITHUB_TOKEN=

# Set the tracker credentials for the WORKFLOW.md tracker.kind you use.
LINEAR_API_KEY=
LINEAR_ASSIGNEE=
JIRA_ENDPOINT=
JIRA_API_TOKEN=
JIRA_EMAIL=
JIRA_ASSIGNEE=
